= simple_form_for(lead, :url => leads_path, :method => 'post',  :html => {:class => 'form-horizontal' }) do |f|
= simple_form_for([:admins, lead], :html => {:class => 'mws-form' }) do |f|
  = f.input :first_name, :label => false

