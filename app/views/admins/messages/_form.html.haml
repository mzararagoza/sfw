= simple_form_for([:admins, message], :html => {:class => 'form-horizontal' }) do |f|
  .mws-form-row
    %label.mws-form-label= t(:message)
    .mws-form-item
    = f.input :message, :label => false
    = f.input :to_addmin
    = f.input :to_girl
  .mws-form-row
    .mws-form-item
    = f.button :submit, :class => 'pull-right btn btn-info'

